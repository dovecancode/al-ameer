import { styled } from '@mui/material'

export const SignUpImage = styled('div')({
  background: "url('LadyDoctor.jpg') no-repeat",
  backgroundSize: 'cover',
  height: '100vh',
  flex: 1,
  backgroundPosition: 'center',
})
