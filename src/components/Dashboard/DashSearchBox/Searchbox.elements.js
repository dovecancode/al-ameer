import styled from '@emotion/styled'
import { TextField } from '@mui/material'

export const Form = styled('form')({
  flex: 0.8,
  marginRight: 'auto',
})

export const SearchBoxField = styled(TextField)(() => ({}))
