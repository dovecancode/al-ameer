import styled from '@emotion/styled'

export const DotCircle = styled('span')({
  display: 'inline-block',
  backgroundColor: '#159EEC',
  borderRadius: '50%',
  width: '1.5rem',
  height: '1.5rem',
  marginRight: '1rem',
})
